# Jetson build settings. Edit this file on the Jetson before running build.sh.

# ON builds the supported ATS adapter and requires AlazarTech ARM64 headers,
# libATSApi.so, and a kernel driver compatible with the active JetPack kernel.
FMCW_JETSON_WITH_ALAZAR=ON
FMCW_JETSON_ALAZAR_SDK_ROOT=/usr/local/AlazarTech

# NVIDIA Jetson AGX Orin uses CUDA compute capability 8.7.
FMCW_JETSON_CUDA_ARCHITECTURES=87

# Enable after the pinned LiDAR_recon source and exported runtime weights have
# been copied to the Jetson. The source may point either to the LiDAR_recon
# repository root or directly to 20_active_count_nms_project.
FMCW_JETSON_WITH_CENTERPOINT=OFF
FMCW_JETSON_CENTERPOINT_SOURCE_DIR=/home/kopti/LiDAR_recon
FMCW_JETSON_CENTERPOINT_WEIGHTS_ROOT=/home/kopti/CenterPoint_Waymo/weights/exported/pointpillars_full_novelocity_epoch12

# Leave empty for the JetPack system cuDNN installation.
FMCW_JETSON_CUDNN_ROOT=

# Leave empty when Qt 6.2 or newer is installed in the system CMake search path.
FMCW_JETSON_QT_ROOT=

# AGX Orin J30 40-pin header UART1: pin 8 TX, pin 10 RX.
FMCW_JETSON_MCU_UART=/dev/ttyTHS0

FMCW_JETSON_BUILD_TESTS=ON
FMCW_JETSON_BUILD_JOBS=auto
FMCW_JETSON_RUN_SMOKE_TEST=ON
